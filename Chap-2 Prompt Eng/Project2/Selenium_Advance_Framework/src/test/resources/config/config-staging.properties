# Staging environment config.
# PRD does not specify a staging host - Insufficient information to determine the real
# staging URL. PLACEHOLDER below must be replaced before this environment is usable.
base.url=https://staging.app.vwo.com
browser=chrome
headless=true
implicit.wait.seconds=5
explicit.wait.seconds=20
page.load.timeout.seconds=30
retry.max.count=2
screenshot.dir=reports/screenshots

# PLACEHOLDER - see config-qa.properties note.
test.valid.email=PLACEHOLDER_VALID_EMAIL@example.com
test.valid.password=PLACEHOLDER_VALID_PASSWORD
